syntax = "proto3";

package socket;

message Socket {
    int32 id = 1;
    string containerName =2 ;
}

message Response {
    bool ok = 1;
}

service SocketService {
    rpc HandleCpuAffinity(Socket) returns (Response) {}
}